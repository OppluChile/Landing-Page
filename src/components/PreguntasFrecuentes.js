export function initAccordion(e=".accordion-header"){let t=document.querySelectorAll(e);t.forEach(e=>{let l=e.querySelector(".flecha-expandir");e.addEventListener("click",()=>{let a="true"===e.getAttribute("aria-expanded");if(t.forEach(e=>{e.setAttribute("aria-expanded","false"),e.nextElementSibling.style.maxHeight=null,e.nextElementSibling.classList.remove("open");let t=e.querySelector(".flecha-expandir");t&&t.classList.remove("efecto-flecha")}),!a){e.setAttribute("aria-expanded","true");let i=e.nextElementSibling;i.style.maxHeight=i.scrollHeight+"px",i.classList.add("open"),l&&l.classList.add("efecto-flecha")}})})}